---
name: DESIGN_FLOW
description: "指导数字 IP/RTL 设计从需求梳理、方案审查与 DE-AI 讨论闭环，到按模块渐进编码和验证收尾。适用于新 IP 启动、设计方案评审或按阶段推进；不用于跳过设计讨论而一次性生成全部 RTL。"
argument-hint: "说明 IP 需求、已有设计文档、当前阶段与完成情况；并列出接口/时钟复位/性能约束、已确定决策和待澄清问题。"
---

# IP 设计工作流

本 skill 用于把一个 IP 的设计过程拆成清晰、可执行、可回溯的3个阶段，使后续每次“开始做一个 design”时，都能按同一套流程推进，而不是直接跳到 RTL。

## 适用场景

- 新建一个 RTL/IP 设计。
- 已有需求文档，需要 DE 先整理设计方案，再和 AI 讨论。
- 需要按模块渐进式编码，而不是一次性把所有 RTL 全写完。
- 需要在编码结束后继续完成通用场景验证和 corner case 验证。

## 核心原则

- 先设计清楚，再写代码；设计讨论没有闭环前，不进入 RTL 编码。
- 先简单模块，后复杂模块；每次只推进一个清晰的编码单元。
- 设计与验证都要分阶段收敛，不允许把“未澄清问题”带到下一阶段。
- AI 对外说明统一使用中文，但不输出内部隐藏推理；只输出结论、依据、实现说明和下一步动作。
- 设计阶段优先发现矛盾、缺口、失败场景和边界条件，而不是默认方案正确。

## 角色分工

### DE 负责

- 阅读和理解需求。
- 手动划分模块、定义接口、明确 feature。
- 手敲设计方案。
- 回答 AI 提出的设计问题。
- 审查每个模块的 RTL 是否符合预期。
- 检查仿真波形是否符合预期。

### AI 负责

- 审查设计方案是否完整、自洽、可实现。
- 将问题整理成可跟踪的问题单并持续追问直到闭环。
- 按模块顺序编写 RTL，并在每个模块结束后停下来等待 DE 审查。
- 编码完成后执行 coding style 和 design tech 检查。
- 搭建验证环境、编写 testcase、运行编译与仿真、配合定位 bug。

## 默认产物

建议在设计过程中维护以下文件；如果文件不存在，AI 可以在用户确认后创建：

- `design_input.md`：DE 手敲的设计方案或需求消化结果，存放于 `stage1` 文件夹。
- `design_checklist.md`：第一阶段勾选项与阶段完成记录，放入 `stage1` 文件夹。
- `design_discussion.md`：第二阶段设计讨论问题、DE 回复、遗留问题闭环记录，放入 `stage2` 文件夹。
- RTL 文件：阶段 3 生成的 RTL 统一放到 `stage3` 文件夹。

## 产物存放路径约定

- 阶段 1 产物统一放到 `stage1` 文件夹。
- 阶段 2 产物统一放到 `stage2` 文件夹。
- 阶段 3 产物统一放到 `stage3` 文件夹。
- 如果目标文件夹不存在，AI 需要先确认是否创建，再生成对应文件。


## 阶段 1：DE 设计准备阶段

**目标：** DE 收集需求、理解需求、完成数据流与模块划分，并手动输出设计方案。

### AI 在本阶段必须执行的动作

- DE 提交设计文档后，AI 先不给出 RTL，也不直接进入方案分析，而是先对文档内容进行审核，调用design_doc_reviewer的SKILL，文档满足要求之后，再要求 DE 逐项确认检查项;
- DE必须充分理解需求;
- DE必须梳理数据流;
- DE必须想到上下游速率不一致时该如何处理;
- DE必须想到corner场景,并想清楚设计如何覆盖;
- DE必须自己手动划分模块;
- DE必须想清楚每个模块的功能;
- DE必须定好顶层接口;
- DE必须定好feature;
- DE必须手敲设计方案;

### 本阶段入口条件

- 用户明确要开始一个新的 IP 设计，或者提供了需求/设计文档。

### 本阶段退出条件

- 满足design_doc_reviewer的SKILL要求的输入内容；
- 上述 9 项全部完成并被 DE 明确确认；
- `design_input.md` 或等价设计方案已经准备好；

如果 design_doc_reviewer的SKILL要求的输入内容有任何一项未完成，AI 必须停留在本阶段，并明确指出还缺什么，不得跳转到下一阶段。
如果 9 项里有任何一项未完成，AI 必须停留在本阶段，并明确指出还缺什么，不得跳转到下一阶段。

## 阶段 2：DE 与 AI 的设计讨论阶段-loop

**目标：** 通过持续问答，让设计方案被充分质疑、修改、补充和收敛，直到双方对模块功能和设计细节都没有歧义。

### 第 1 轮讨论

只有在阶段 1 全部打勾后，AI 才能开始分析设计方案。分析时至少必须回答以下问题：

- 设计方案有哪些描述不清晰的地方？
- 设计方案有哪些互相矛盾的地方？
- 设计方案在哪些场景下会失败？
- 设计方案有哪些思路可以优化？

AI 需要将问题整理并导出到 `design_discussion.md`，通知 DE 逐项解答。

### 后续讨论循环

1. DE 阅读 `design_discussion.md` 并逐项回复。
2. AI 再次审查 DE 的回复和整体设计。
3. 如果仍有问题，AI 必须把新问题继续追加到 `design_discussion.md`。
4. 通知 DE 继续回答。
5. 重复上述流程，直到 AI 没有新的问题。


### 本阶段硬性要求
AI必须质疑方案,不能默认方案正确
新问题必须是增量问题,避免重复追问已经澄清的点
未闭环的问题不得直接忽略
如果发现缺少关键设计信息,AI必须明确标出"阻塞项"

### 本阶段退出条件
design_discussion.md中所有关键问题都已经闭环
设计方案不再存在明显歧义、矛盾或未定义行为
AI明确确认可以进入RTL编码


## 阶段 3：AI编码阶段

目标:AI按模块逐步写RTL,并在每一轮编码后停下来等待DE审查,直到所有模块收敛。
编码顺序要求:
- 必须先写简单模块,再写复杂模块
- 每次优先完成一个模块或一个清晰功能块,不要一口气把所有模块全部写完 
- 每写完一个模块,AI必须停下来提醒DE审查

编码交互循环:
- AI编写一个模块,并用中文给出简洁的实现说明、接口说明和关键设计取舍
- AI生成的RTL文件必须放到stage3文件夹
- DE检查该模块是否符合预期
- 如果不符合,DE指出不符合要求的地方
- AI根据反馈重写该模块
- DE再次检查
- 如此循环,直到DE明确认可该模块


阶段收尾检查:
所有模块完成后,AI必须再做两次整体检查:
- 按RTL_coding_style做一次整体codingstyle检查
- 按RTL_design_tech做一次整体designtech检查

本阶段硬性要求:
- AI不得在DE尚未确认当前模块时继续无条件展开下一个复杂模块;
- 对外说明使用中文,但不输出内部隐藏推理;
- 如果某个模块需求仍然不清晰,AI必须退回阶段2的讨论思路,而不是带着猜测写RTL;

本阶段退出条件:
- 所有模块都经过DE审查并认可;
- 已完成整体codingstyle检查;
- 已完成整体designtech检查;

## AI 执行模板

当用户说“开始做一个 IP 设计”时，AI 默认按以下顺序推进：

1. 先确认当前处于哪个阶段；如果用户没说，默认从阶段 1 开始。
2. 明确当前阶段的输入、输出和退出条件。
3. 只执行当前阶段应该做的动作，不抢跑后续阶段。
4. 每轮结束时告知 DE：当前结论、待回答问题、下一步动作。

## 与其他 skill 的配合关系

- 进入 RTL 编码和 RTL review 时，配合 `RTL_coding_style`。
- 做架构审视、模块拆分、计数器/FSM/FIFO 等设计审查时，配合 `RTL_design_tech`。
